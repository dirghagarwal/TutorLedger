"use client";

import { useState } from "react";
import { updatePaymentAction } from "@/app/actions/payments";
import { Button } from "@/components/ui/button";
import { Dialog, DialogContent, DialogDescription, DialogFooter, DialogHeader, DialogTitle } from "@/components/ui/dialog";
import { Input } from "@/components/ui/input";
import { BillingPeriod, PaymentMethod, PaymentStatus, type Payment } from "@/types/payment";

export default function EditPaymentDialog({ payment, studentName, onClose, onSaved }: { payment: Payment; studentName: string; onClose: () => void; onSaved: () => void }) {
  const [amount, setAmount] = useState(String(payment.amount)); const [date, setDate] = useState(payment.date); const [method, setMethod] = useState<PaymentMethod>(payment.method); const [status, setStatus] = useState<PaymentStatus>(payment.status); const [billingPeriod, setBillingPeriod] = useState(payment.billingPeriod); const [coveredMonth, setCoveredMonth] = useState(payment.coveredMonth ?? ""); const [from, setFrom] = useState(payment.coveredFromDate ?? ""); const [to, setTo] = useState(payment.coveredToDate ?? ""); const [count, setCount] = useState(payment.coveredClassCount ? String(payment.coveredClassCount) : ""); const [notes, setNotes] = useState(payment.notes); const [error, setError] = useState(""); const [saving, setSaving] = useState(false);
  const submit = async (e: React.FormEvent) => { e.preventDefault(); setSaving(true); setError(""); const res = await updatePaymentAction({ paymentId: payment.id, amount: Number(amount), date, method, status, billingPeriod, coveredMonth: billingPeriod === BillingPeriod.MONTHLY ? coveredMonth : null, coveredFromDate: billingPeriod === BillingPeriod.CLASSWISE ? from : null, coveredToDate: billingPeriod === BillingPeriod.CLASSWISE ? to : null, coveredClassCount: billingPeriod === BillingPeriod.CLASSWISE ? Number(count) : null, notes }); setSaving(false); if (!res.ok) { setError(res.error); return; } onSaved(); };
  return <Dialog open onOpenChange={(open) => { if (!open) onClose(); }}><DialogContent className="w-[calc(100%-1rem)] border-border-strong bg-surface text-foreground sm:max-w-lg"><DialogHeader><DialogTitle>Edit payment</DialogTitle><DialogDescription>{studentName} · update the transaction and the tuition period it covers.</DialogDescription></DialogHeader><form onSubmit={(e) => void submit(e)} className="grid max-h-[78dvh] gap-4 overflow-y-auto pr-1"><div className="grid gap-3 sm:grid-cols-2"><label className="grid gap-1.5 text-sm font-medium">Amount<Input type="number" min={1} value={amount} onChange={(e) => setAmount(e.target.value)} /></label><label className="grid gap-1.5 text-sm font-medium">Received date<Input type="date" value={date} onChange={(e) => setDate(e.target.value)} /></label></div><div className="grid gap-3 sm:grid-cols-3"><label className="grid gap-1.5 text-sm font-medium">Method<select className="h-11 rounded-lg border border-input bg-background px-2" value={method} onChange={(e) => setMethod(e.target.value as PaymentMethod)}><option value={PaymentMethod.UPI}>UPI</option><option value={PaymentMethod.CASH}>Cash</option><option value={PaymentMethod.BANK_TRANSFER}>Bank transfer</option><option value={PaymentMethod.CARD}>Card</option></select></label><label className="grid gap-1.5 text-sm font-medium">Status<select className="h-11 rounded-lg border border-input bg-background px-2" value={status} onChange={(e) => setStatus(e.target.value as PaymentStatus)}><option value={PaymentStatus.PAID}>Paid</option><option value={PaymentStatus.PARTIAL}>Partial</option><option value={PaymentStatus.PENDING}>Pending</option></select></label><label className="grid gap-1.5 text-sm font-medium">Billing<select className="h-11 rounded-lg border border-input bg-background px-2" value={billingPeriod} onChange={(e) => setBillingPeriod(e.target.value as BillingPeriod)}><option value={BillingPeriod.MONTHLY}>Monthly</option><option value={BillingPeriod.CLASSWISE}>Class-wise</option></select></label></div>{billingPeriod === BillingPeriod.MONTHLY ? <label className="grid gap-1.5 rounded-2xl border border-primary/15 bg-primary/5 p-4 text-sm font-medium">Month covered<Input type="month" value={coveredMonth} onChange={(e) => setCoveredMonth(e.target.value)} /></label> : <div className="grid gap-3 rounded-2xl border border-primary/15 bg-primary/5 p-4 sm:grid-cols-3"><label className="grid gap-1.5 text-xs font-medium">From<Input type="date" value={from} onChange={(e) => setFrom(e.target.value)} /></label><label className="grid gap-1.5 text-xs font-medium">To<Input type="date" value={to} onChange={(e) => setTo(e.target.value)} /></label><label className="grid gap-1.5 text-xs font-medium">Classes<Input type="number" min={1} value={count} onChange={(e) => setCount(e.target.value)} /></label></div>}<label className="grid gap-1.5 text-sm font-medium">Notes<textarea className="min-h-20 rounded-lg border border-input bg-background p-2" value={notes} onChange={(e) => setNotes(e.target.value)} /></label>{error && <p className="rounded-xl bg-destructive/10 p-3 text-sm text-destructive">{error}</p>}<DialogFooter><Button type="button" variant="ghost" onClick={onClose}>Cancel</Button><Button disabled={saving} type="submit">{saving ? "Saving…" : "Save payment"}</Button></DialogFooter></form></DialogContent></Dialog>;
}
